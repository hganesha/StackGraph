[project]
name = "ledgerkit"
version = "1.3.0"
description = "Double-entry ledger primitives."
dependencies = [
  "attrs>=23.1",
  "python-dateutil==2.8.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
