route:
  receiver: production-webhook
  group_by: [alertname, owner]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 2h
receivers:
  - name: production-webhook
    webhook_configs:
      - url: "__STACKGRAPH_ALERT_WEBHOOK_URL__"
        send_resolved: true
